@page
@model parcial_Horta_Gomez.Pages.Servicios.DeleteModel
@{
    ViewData["Title"] = "Eliminar Servicio";
}
<form method="post" class="bg-white p-10">

    <div class="mb-4">
        <label for="nombre" class="block text-gray-700 text-sm font-bold mb-2">
            Nombre:
        </label>
        <p>
            @Html.DisplayFor(model => model.Servicio.Name)
        </p>
    </div>

    <input type="hidden" asp-for="Servicio.Id"/>
    <div class="flex gap-2">
        <button type="submit" class="bg-red-500 hover:bg-red-600 text-white rounded-lg flex items-center font-bold py-2 px-4 focus:outline-none focus:shadow-outlines">
            Eliminar
        </button>
        <a asp-area="" asp-page="/Servicios/Index" class="bg-black hover:bg-black/50 text-white rounded-lg flex items-center font-bold py-2 px-4 focus:outline-none focus:shadow-outline">
            Regresar
        </a>
    </div>
</form>